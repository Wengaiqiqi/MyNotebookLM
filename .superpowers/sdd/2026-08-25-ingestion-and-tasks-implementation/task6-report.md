# Task 6 实现报告

## 完成内容

- 新增 UTF-8 TXT 解析：处理 BOM、CRLF/LF、空段落，并生成段落定位。
- 新增 Markdown 解析：生成标题层级、标题祖先、列表、代码和表格文本块。
- 新增 CSV 解析：检测常见分隔符，保留带引号的逗号与换行，输出表头映射和 CSV 行定位。
- 为三类解析器增加 golden 测试、规范化快照和文档 fixture。
- 根据独立复审修复 CSV 空行后的物理行号、稳健分隔符检测；修复 Markdown 代码块 kind、连续多行段落、半开 offset 和表格误识别。

## TDD 记录

复审修复严格先补回归测试并确认 3 项行为 RED；随后补充最小实现，聚焦测试 GREEN；最后更新快照并保持相关 parser 全量测试通过。

## 验证

- 聚焦测试：3 个测试文件、6 个测试通过。
- 相关 parser 全量：7 个测试文件、10 个测试通过。
- typecheck：通过。
